# learning-kotlin
To learn the Kotlin language syntax.

## Why doesn't it use DevContainers extension of VS Code ?

This project is to learn Kotlin syntax and Spring Boot. VS Code extension can't handle Kotlin version 2.xx. Therefore, we can't use the feature of supplementing code syntax.<br>
So we have to install JDK and Kotlin standard libraries on the local laptop environment.
